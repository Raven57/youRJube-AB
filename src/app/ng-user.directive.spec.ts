import { NgUserDirective } from './ng-user.directive';

describe('NgUserDirective', () => {
  it('should create an instance', () => {
    const directive = new NgUserDirective();
    expect(directive).toBeTruthy();
  });
});
